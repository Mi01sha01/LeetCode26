## [410.Split Array Largest Sum](https://leetcode.com/problems/split-array-largest-sum/)

## Description

Find the minimum possible largest subarray sum after splitting the array into `k` non-empty subarrays by using binary search on the answer with a greedy validation.

## Complexity

- Time: O(n log(sum(nums) - max(nums)))
- Space: O(1)